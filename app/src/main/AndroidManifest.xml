<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Root manifest file for the Memento app -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Memento"
        tools:targetApi="31">

        <!--
            Main launcher activity.
            - android:exported="true" is required for apps targeting Android 12+.
            - This is the entry point of the app.
        -->
        <activity android:name=".AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />

        <activity android:name=".ProfileActivity" />

        <!-- Mapbox access token (visible in APK, use with caution in production) -->
        <meta-data
            android:name="com.mapbox.maps.token"
            android:value="pk.eyJ1IjoidG9tbzE1MyIsImEiOiJjbWI2ZmxsYTkwMGNhMmtzZDhnbmF3cnRlIn0.IWyqpTzgLl-zj89kcEAYoQ" />

    </application>

</manifest>